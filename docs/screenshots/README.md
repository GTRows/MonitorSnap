Place these screenshots here:
- presets.png  (main window showing the Presets page with a selected preset)
- displays.png (Displays page showing the monitor layout)
- tray.png     (system tray context menu expanded)

Recommended: capture at 1080p window size, dark theme, with 2-3 presets saved so the UI looks populated. PNG, no borders.
